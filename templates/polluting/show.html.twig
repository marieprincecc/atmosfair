{% extends 'base.html.twig' %}

{% block title %}Polluting{% endblock %}

{% block body %}
    <h1>Polluting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ polluting.id }}</td>
            </tr>
            <tr>
                <th>Subst</th>
                <td>{{ polluting.subst }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('polluting_index') }}">back to list</a>

    <a href="{{ path('polluting_edit', {'id': polluting.id}) }}">edit</a>

    {{ include('polluting/_delete_form.html.twig') }}
{% endblock %}
