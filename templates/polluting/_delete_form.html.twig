<form method="post" action="{{ path('polluting_delete', {'id': polluting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ polluting.id) }}">
    <button class="btn">Delete</button>
</form>
